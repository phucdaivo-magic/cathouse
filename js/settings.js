// Init global namespace
window.Panthera = {};

// On non-proudction environments, there is more output on the consolve
window.Panthera.IS_PRODUCTION = true;

// API endpoint where the CMS is accesible.
window.Panthera.API_ENDPOINT = '/cms';

// Assets path, change if served through cloudfront CDN
window.Panthera.ASSET_PATH = 'http://localhost:3004/';

